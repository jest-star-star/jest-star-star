export default class Queue {
	size = 0;

	#private_variable;

	constructor(iterable) {
		// this.#private_variable = #private_method();
	}

	#private_method() {
		return 'foo';
	}

	get size() {
		return size;
	}
}

// get red() {
// 	return this.values[0];
// }
//
// set red(value) {
// 	this.values[0] = value;
// }

// if (!(#values in anotherColor)) {
// 	throw new TypeError("Color instance expected");
// }
